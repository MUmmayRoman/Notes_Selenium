package training_Selenium;

public class Log4jTest {
	
	/*
	 * Log4j---> It is used for the loggin of Results.
	 * 
	 * Four Major components of Log4j:--
	 * 1.Logmanager-->It is considered as the factory for producing the logger objects. 
	 * 2.Logger-->It is the actual logger.
	 * 3.Appenders-->It is used to save the results in different formates.Like html, console, textfile and database. 
	 * 4.Layout-->It is used for the design pattern.
	 * 
	 * pre-requisite:--Need to have a log4j jar file.
	 * 
	 * Log4j can be achived by two ways:- 
	 * 1.Using properties file. 
	 * 2.Using xml file.
	 */
	

}
